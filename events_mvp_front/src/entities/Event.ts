export default interface Event {
  id: number;
  name: string;
  capacity: number;
  startDateTime: string;
  locationName: string;
  locationAddress: string;
  description: string;
  posterImage: string;
}
